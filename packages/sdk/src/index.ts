// TODO: Export all public SDK modules here.
// Modules to implement:
//   - ./passkey   — Passkey Kit wrapper (secp256r1 smart wallet creation)
//   - ./contract  — Soroban contract client (quiz-pool + leaderboard bindings)
//   - ./relayer   — Relayer HTTP client (submit signed XDR)
//   - ./types     — Shared TypeScript types
